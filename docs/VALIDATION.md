# 本地验收记录

2026-09-13

- TypeScript 检查、Cloudflare Worker dry-run 构建通过；未发布到云端。
- 本地测试 16 项通过，包含发布、下架、中文搜索、链接校验、反馈、导入、上传和生产配置缺失时后台拒绝访问。
- 浏览器验证搜索、详情、后台添加链接和保存草稿成功，无控制台错误。
- 390px 手机宽度：首页、详情、编辑表单没有页面横向溢出。
- 浏览器验证草稿已清理。上传验证在本地 R2 留有少量 1px PNG 图片，不影响生产。

上线还需配置用户自己的 Cloudflare 账号、域名、管理员 Access 权限及数据库，验证实际登录流程，并添加真实夸克链接。演示资料没有真实下载链接。部署步骤见 DEPLOY.md。

## PanSou 接入验收 · 2026-09-14

- 已下载指定仓库并从源码构建本机 arm64 Go 程序。
- 单个 `npm run dev` 同时启动网站和 PanSou，两个端口仅监听本机。
- 实际搜索 Python 返回 22 条夸克资源（本次结果来自 PanSearch 和 yunpanx），浏览器已显示；其他来源本次返回 0 条，不等于来源故障或永远无结果。
- 浏览器搜索“简历”只显示本站资源；网络接口也会重新检查本地命中，防止绕过优先规则。
- 搜索设置显示 3 个插件与 2 个频道；390px 宽度无横向溢出。
- 21 个测试通过，1 个生产 Access 测试因未运行生产模式验证服务而跳过；TypeScript 和 Worker dry-run 通过。
- Cloudflare 容器配置已编写，但本机无 Docker，尚未完成容器镜像构建和云端联调。Worker dry-run 显式跳过容器 rollout，不代表镜像构建通过。
